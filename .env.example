# Where session requests are emailed. Leave these unset and the site still
# works, the request just lands in the server logs instead.
RESEND_API_KEY=
LEAD_EMAIL=
# Optional. Needs a domain verified with Resend before you can change it.
LEAD_FROM="The Advisor <onboarding@resend.dev>"
